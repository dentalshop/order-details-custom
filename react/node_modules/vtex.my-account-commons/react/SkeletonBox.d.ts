import { FunctionComponent } from 'react';
declare const SkeletonBox: FunctionComponent<Props>;
interface Props {
    maxWidthStep?: number;
    shouldAllowGrowing?: boolean;
    shouldShowLowerButton?: boolean;
}
export default SkeletonBox;
